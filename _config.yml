# GitHub Pages configuration
# This tells GitHub Pages to serve the site from the root of the repo

theme: null          # no Jekyll theme — we're serving raw HTML
include:
  - index.html
  - README.md
